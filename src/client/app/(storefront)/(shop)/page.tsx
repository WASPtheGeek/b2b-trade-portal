"use client";

// Main home page.
export default function HomePage() {

  return (
    <div
      className="relative min-h-screen"
      style={ { background: "linear-gradient(180deg, var(--orange-50) 0%, var(--surface-warm) 32%, var(--surface-warm) 100%)" } }
    >
    </div>
  );
}